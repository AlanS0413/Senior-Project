//- @pugdoc
    name: brandproducts.pug
    description: Brand Products Template
                 This Pug template defines the layout and content for displaying products of a specific brand.
                 It extends the 'layout.pug' template and includes mixins to render lists of products and brand options.
                 The comments below explain the structure and purpose of each section.

//- Extending the 'layout.pug' template
extends layout.pug

//- @pugdoc
     name: jordanBrand Mixin
     description: Define a mixin for rendering products of a specific brand (Jordan brand, in this case)
     arguments:
       - {array} brandProducts - An array of products for the specific brand
     examples:
       - |
         +jordanBrand([
           { name: 'Jordan Product 1' },
           { name: 'Jordan Product 2' }
         ])

mixin jordanBrand(brandProducts)
    ul
        each jordan in brandProducts
            li
                //- Product details for Jordan brand can be added here

//- @pugdoc
     name: brandOptions Mixin
     description: Define a mixin for rendering brand options (e.g., different types of products for a brand)
     arguments:
       - {array} brandTypes - An array of different types of products for the brand
     examples:
       - |
         +brandOptions([
           { type: 'Item Type 1' },
           { type: 'Item Type 2' }
         ])

mixin brandOptions(brandTypes)
    ul
        each types in brandTypes
            li
                //- Brand type options can be added here

//- Define the 'content' block for this template
block content
    body
        //- Container for displaying brand products
        .container.rounded.text-center
            //- Unordered list for displaying product details of a specific brand
            ul#brandproducttable.text-center.text-black.justify-content-center

            //- Include Bootstrap JS script for functionality
            script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js", integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM", crossorigin="anonymous")

            //- Include custom JavaScript functions for functionality
            script.
                //- Load brand products using JavaScript function
                loadBrandProducts();
                //- Search for items using JavaScript function
                searchItems();
                jQuery(document).ready(function() {
                    //- Use jQuery to apply thumbnail styling to images
                    jQuery('img.center-img.thumbnails').nailthumb({width:335,height:185, method:'resize'});
                });

